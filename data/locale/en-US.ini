SourceName="Selective Fullscreen"
Monitor="Monitor"
ProcessList="Applications"
ProcessList.Tooltip="One entry per application. Append |text to only capture windows whose title contains the text (case-insensitive), e.g. chrome.exe|My Stream."
AddProcess="Add running application"
AddProcess.None="-- select to add --"
CaptureCursor="Capture cursor"
MatchFullPath="Match full executable path instead of file name"
PauseWhenHidden="Pause capture while the source is hidden"
ScaledLayout="Fit layout to canvas (keep window pixel size)"
ScaledLayout.Tooltip="Report the source at the target/canvas size instead of the monitor size, and remap each window's desktop position proportionally while keeping its native pixel size, so windows stay readable when the canvas is smaller than the monitor."
ShrinkOversized="Shrink windows larger than the canvas to fit (otherwise clip at edges)"
TargetWidth="Target width"
TargetHeight="Target height"
TargetSize.Tooltip="Canvas size to map windows into. 0 = match the OBS base (canvas) resolution."
